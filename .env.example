# Copy to .env and replace both credential placeholders before startup.
# .env is ignored by Git. Do not commit real credentials.
MONGO_INITDB_ROOT_USERNAME=replace-with-your-mongo-username
MONGO_INITDB_ROOT_PASSWORD=replace-with-a-unique-password
MONGO_DB=techstock_db
